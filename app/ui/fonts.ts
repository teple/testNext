import { Inter, Lusitana, Rubik_Marker_Hatch } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
 
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});

export const rubik_marker_hatch = Rubik_Marker_Hatch({
    weight: '400',
    subsets: ['latin'],
  });